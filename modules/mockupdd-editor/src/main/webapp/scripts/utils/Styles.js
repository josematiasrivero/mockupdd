/**
 * This enum has all the Twitter Bootstrap styles for the app. 
 */

var Styles = {
  PRIMARY : "primary",
  INFO : "info",
  WARNING : "warning",
  DANGER : "danger",
  DEFAULT : "default",
  LINK : "link",
  SUCCESS : "success"
};

if(Object.freeze){
  Object.freeze(Styles);
}